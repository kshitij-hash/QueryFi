{
  "name": "queryfi-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @queryfi/app dev",
    "build": "pnpm --filter @queryfi/app build",
    "lint": "pnpm --filter @queryfi/app lint",
    "test:contracts": "cd packages/contracts && forge test"
  }
}
